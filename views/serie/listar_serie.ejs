<script src="/js/serie/excluir.js"></script>

<!-- Main Section -->
<div class="col-md-9">
    <section class="mb-5">
        <h4 class="text-primary">Séries</h4>

        
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th><input type="checkbox" id="ckTodos"></th>
                    <th>ID</th>
                    <th>Serie</th>
                    <th>Ano Letivo</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                <%for(let i=0; i < lista_series.length; i++){%>
                    <tr>
                        <td><input type='checkbox' class='check_del' data-id="<%= lista_series[i].id%>" data-name="<%= lista_series[i].serie%>"></td>
                        <td><%= lista_series[i].id%></td>
                        <td><%= lista_series[i].serie%></td>
                        <td><%= lista_series[i].ano_letivo%></td>
                        <td>
                            <a href="/serie/editar/<%= lista_series[i].id%>" class="btn btn-primary">
                                Editar
                            </a>
                            <button class="btn btn-danger excluir" data-id="<%= lista_series[i].id%>" data-name="<%= lista_series[i].serie%>">Excluir</button>
                        </td>
                    </tr>
                <%}%>
            </tbody>
        </table>
        <div>
            <a href="/serie/cadastrar" class="btn btn-primary">Cadastrar Série</a>
            <input type="button" class="btn btn-danger" value="excluir selecionados" id="btnExcluirSelecionados">
        </div>
    </section>
</div>
<!-- Content End -->